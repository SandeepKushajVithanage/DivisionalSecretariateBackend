import { createTheme } from "@mui/material"

const theme = createTheme({
    palette: {
      primary: {
        main: '#fac134',
      },
      secondary: {
        main: '#DD7E00',
      },
    },
  })

export default theme